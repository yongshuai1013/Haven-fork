- **Cloudflare Access sign-in now actually works with self-hosted applications.** The v5.89.2 fix never ran on a device: the probe fetching the login redirect did blocking network I/O on the main thread, Android killed it, the failure was swallowed, and sign-in fell back to the constructed URL self-hosted apps reject (#643). The probe now runs on a worker thread.