When an AI agent (over Haven's MCP endpoint) asks to do something that needs your approval but Haven is in the background, you now get a tap-to-open notification instead of the request silently failing. Tap it to bring Haven up and allow or deny — the action still can't proceed without your explicit OK, so this just makes sure you're asked rather than left wondering why an agent went quiet.
