SSH terminal: blank screen on return

An SSH terminal no longer comes back blank after the app is backgrounded and the system reclaims it — the screen is now restored on return instead of staying empty until you press a key. This is the SSH counterpart of the local-terminal fix in 5.59.98 (#272).
